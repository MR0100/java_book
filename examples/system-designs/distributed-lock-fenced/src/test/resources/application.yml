# Test profile. Disable the live demo CommandLineRunner so loading the Spring
# context for @SpringBootTest does NOT fire the Redis-dependent narration; the
# integration test drives the lock directly instead.
demo:
  enabled: false

logging:
  level:
    com.javamastery.examples.lock: INFO
