# Maven build output
target/

# Shade plugin byproduct (suppressed via createDependencyReducedPom, kept here as belt-and-braces)
dependency-reduced-pom.xml

# JMH machine-readable result dumps (e.g. -rf json -rff out.json)
*.json
